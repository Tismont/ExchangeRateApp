import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  page: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  currency: {
    fontSize: 60,
    color: 'whitesmoke',
    fontWeight: 'bold',
    bottom: 70,
  },
  exchangeRate: {
    fontSize: 90,
    color: 'whitesmoke',
    fontWeight: '200',
  },
});
